spring:
  # 环境 dev|test|prod
  profiles:
    active: dev
  application:
    name: ware-service
  cloud:
    nacos:
      discovery:
        server-addr: 116.85.44.154:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 10600