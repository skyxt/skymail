spring:
  # 环境 dev|test|prod
  profiles:
    active: dev
  application:
    name: coupon-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 10200
